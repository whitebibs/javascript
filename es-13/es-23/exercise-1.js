const person = {
  firstName: 'Mario',
  lastName: 'Rossi',
  age: 25
}
// Print values of person using Object.keys


console.log(Object.keys(person)); //chiavi
console.log(Object.values(person)); //valori
console.log(Object.entries(person))//array con 2 elementi(chiave e valore)


/*const key= Object.keys(person);
console.log(key);
const val=Object.values(person);
console.log(val);
const entry=Object.entries(person);
console.log(entry);
*/
